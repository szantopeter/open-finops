{
  "name": "pricing-generator-cli",
  "version": "0.1.0",
  "description": "Standalone CLI for pricing-generator tools folder",
  "bin": {
    "pricing": "dist/index.js"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "start": "npm run build && node dist/index.js"
  },
  "devDependencies": {
    "@aws-sdk/client-sts": "^3.0.0",
    "@aws-sdk/shared-ini-file-loader": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
